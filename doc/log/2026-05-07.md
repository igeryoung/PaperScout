# Log — 2026-05-07

## Phase 0 — scaffold green, awaiting Docker + browser

**Done:**

- Scaffolded Next.js 16.2.5 (TypeScript + App Router + Tailwind v4 + ESLint + Turbopack) via `/tmp/pcs-scaffold-tmp` then `cp -a` into project (avoids mv-aside)
- Renamed `package.json` to `paper-collection-system`; added scripts: `test`, `test:watch`, `test:e2e`, `format`, `format:check`, `db:seed`, `prisma:generate|migrate|studio`, `postinstall`
- Pinned Prisma to **v6** (v7 dropped datasource `url`; bigger refactor for no Phase 0 benefit)
- Installed: `zod`, `pino`, `prisma@6`, `@prisma/client@6`, `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/{react,jest-dom}`, `@playwright/test`, `prettier`, `prettier-plugin-tailwindcss`, `eslint-config-prettier`, `tsx`, `p-limit`, `clsx`, `tailwind-merge`, `lucide-react`, `class-variance-authority`
- Wrote `src/lib/{env,db,logger,utils}.ts` with `'server-only'` boundary; `env.ts` uses zod to validate `DATABASE_URL`, `ANTHROPIC_API_KEY`, `LOG_LEVEL`, `NODE_ENV`
- Wrote `prisma/schema.prisma` (empty models — Phase 1 will fill); `prisma generate` succeeds
- Wrote `vitest.config.ts`, `playwright.config.ts`, `.prettierrc.json`; updated `eslint.config.mjs` to include `eslint-config-prettier` and ignore `src/components/ui/**`
- Sample test in `tests/unit/sample.test.ts` passes
- shadcn: wrote `components.json` directly to skip interactive init; ran `shadcn add` for 11 baseline primitives → `src/components/ui/*.tsx`
- Wrote `docker-compose.yml` (postgres:16-alpine, healthcheck, named volume), `.env.example`, project `README.md`; updated `.gitignore` to allow `.env.example`
- `npm run lint`, `npm test`, `npm run build` all green and clean

**Deviations from plan:**

- **Move-aside replaced with subdirectory scaffold.** `create-next-app` insisted on an empty target dir; rather than `mv doc/ .omc/ plan/` aside, we scaffolded into `/tmp/pcs-scaffold-tmp` then `cp -a` into the project. Same end state, no mv of existing project artifacts.
- **Prisma pinned to v6 instead of latest.** v7 dropped `url` from datasource (now `prisma.config.ts` + adapter pattern). Pinning to v6 keeps the well-known `url` in schema and matches every Prisma tutorial out there. Revisit when v7 ecosystem matures.
- **Tailwind v4 has no `tailwind.config.ts`.** Configuration moved to CSS via `@import "tailwindcss"` and `@theme` directives. The Phase 0 plan mentioned `tailwind.config.ts` — this file does not exist by design in v4 and is not needed.
- **`next.config.ts` not `next.config.mjs`.** Next 16's `create-next-app` ships TypeScript config by default. Functionally equivalent.

**Surprises / learnings:**

- `create-next-app` ignored `--no-git` and initialized a `.git`. Cleaned it up before the merge.
- shadcn `add` does not auto-install `clsx` / `tailwind-merge` / `lucide-react` / `class-variance-authority` — only Radix packages. Had to install those manually before primitives compile.
- `shadcn init --yes` is still interactive (asks Radix vs Base). Bypassed by writing `components.json` directly.
- `AGENTS.md` shipped by Next 16 explicitly warns that the framework has breaking changes vs training data and that agents should consult `node_modules/next/dist/docs/` before writing Next-specific code. Future phases should heed this.

**Next:** Awaiting user actions — start Docker Desktop, run `docker compose up -d`, verify `localhost:3000` in a browser, and decide on `git init` + first commit. Once those are green, Phase 0 closes and Phase 0.5 — PoC begins.

---

## Phase 0 — handoff convention scaffolded

**Done:**

- Wrote `plan/STATE.md` with initial pointer at Phase 0 — Bootstrap
- Created `plan/log/` directory and this first entry
- Appended `STATE.md` + `log/YYYY-MM-DD.md` update boxes to the Verification checklist of every `plan/phase-*/README.md` (eight files)
- The convention `STATE.md` (overwritten/session) + `plan/log/YYYY-MM-DD.md` (append-only, one file per work day) is now part of every phase's exit criteria

**Deviations from plan:** none — matches the approved Agent Handoff Convention section in `~/.claude/plans/base-on-doc-prd-v1-md-build-serialized-sedgewick.md`.

**Surprises / learnings:** none yet.

**Next:** Begin Phase 0 implementation — move-aside `doc/` and `.omc/`, run `create-next-app`, restore the moved-aside dirs. See `plan/phase-0-bootstrap/README.md` for the full 12-step checklist.
