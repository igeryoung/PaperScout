# Log — 2026-05-11

## Phase 4 — Paper trend UI closed

- Built the read-only Phase 4 viewer over completed runs. App is now a viewer for ingested skill output; no Generate button, no fake stages, no Phase-5 feedback.
- **View-model layer** — new `src/server/lib/select-evaluation.ts` mirrors `scripts/ingest/lib.ts:chooseRankingScore` against `PaperEvaluation` DB rows (`FULL_PDF SUCCESS` > `ABSTRACT_SCREENING` > `FULL_PDF FAILED/UNAVAILABLE`); same file owns `scoreTier()` (good/mid/weak at 70%/50%/0 ratios). Per the planning step, aggregates live in a new `src/server/repos/trends.ts` rather than being grafted onto `runs.ts`: `trendsRepo.getRunSummary(runId)` returns `{ totalPapers, recommendedCount, sources[], topTags[], scoreStats { min,median,max }, pdfStatus { success,failed,unavailable,none } }` with median computed in JS over the *best* eval per paper. `runResultsRepo.findByRunWithDetail` and `papersRepo.findDetailById` join `paper → { evaluations, tags, sources, codeLinks }` with `recommendedOnly` opt-in; both expose `Prisma.*GetPayload`-derived types so pages don't reshape.
- **Pages** — `src/app/page.tsx` swapped the boilerplate for the trend dashboard (`TrendSummary` + `TrendTags` + `SourceMix` + top recommended `PaperCard`s + "View full run" link); empty state matches the README copy and points at `npm run ingest <run-dir>`. `src/app/runs/[id]/page.tsx` branches on `RunStatus` (RUNNING/FAILED/COMPLETED) without inventing fake substages; `?showAll=1` flips `findByRunWithDetail` to all results. `src/app/papers/[id]/page.tsx` calls `findDetailById` and renders `<PaperDetail>`; backlink resolves to the latest completed run or `/library`. All pages are Server Components with `dynamic = 'force-dynamic'` + `import 'server-only'`.
- **Components (all Server Components)** — `app-header.tsx` (PaperScout brand + Home/Library + Latest run when present, resolved server-side from `runsRepo.latestCompleted`), `trend-summary.tsx`, `trend-tags.tsx` (caps at 12 with `+N more`, tags link to `/library?tags=...` as a Phase 5 hand-off), `source-mix.tsx` (segmented bar + legend, collapses zero slices), `paper-card.tsx` (rank, title→`/papers/[id]`, authors truncated to 3 + "et al.", source/stage/decision badges, tags max 5, clamped summary + recommendationReason, conditional PDF/source/code action row), `paper-detail.tsx` (everything in the card plus abstract, keyContribution, methodologySummary, strengths, weaknesses, ranking note, link list — guards every `evaluation?.field` so ABSTRACT_SCREENING-only papers render cleanly), `score-breakdown.tsx` (5 dimension bars + total with tier color). Shared formatters live in `src/lib/format.ts` (`formatDate`, `formatAuthors`, `stringsFromJson` for `Json` columns).
- **Root layout** — `<AppHeader />` is now rendered globally in `src/app/layout.tsx`, so `/library` picks it up too. Metadata changed from "Create Next App" to "PaperScout".
- **Loading + error** — `src/app/loading.tsx` (dashboard skeleton), `src/app/runs/[id]/loading.tsx` (ranked-list skeleton), `src/app/papers/[id]/loading.tsx` (detail skeleton); `src/app/error.tsx` is the lone `'use client'` file with the `reset()` button.
- **Tests** — `tests/unit/server/select-evaluation.test.ts` (10 tests) covers every selection path (none / abstract only / FULL_PDF SUCCESS over abstract / FULL_PDF FAILED falling back to abstract / FULL_PDF UNAVAILABLE alone — the F5 path) and `scoreTier()` thresholds. `tests/integration/ui-repos.test.ts` (4 tests, env-gated) re-ingests the frozen Phase 2.5 reference run and asserts on `trendsRepo.getRunSummary` (5 papers / 3 recommended / arXiv=5 / pdfStatus: 4 SUCCESS + 1 UNAVAILABLE / min=30 median=71 max=86 / top-tags monotonic), `runResultsRepo.findByRunWithDetail` (3 recommended w/ joined evaluations + sources, 5 with `showAll`), and `papersRepo.findDetailById` (non-null + relations populated; null for bogus uuid).
- **vitest config** — `test:integration` npm script gained `--no-file-parallelism`. Adding a second test file that touches the reference fixture exposed a latent collision: ingest.test.ts and ui-repos.test.ts both `TRUNCATE` + reinsert into `paperscout_test`, and parallel files were interleaving truncates with the other file's inserts. Serial integration runs are the right invariant for any DB-touching test against a single schema; cost is ~3s of total wall time.
- **Verification** — `npm run lint` clean; `npx tsc --noEmit` clean; `npm test` 117/117 (was 107 + 10 select-evaluation); `npm run test:integration` 11/11 (was 7 + 4 ui-repos); `npm run build` green with `/`, `/runs/[id]`, `/papers/[id]` registered as ƒ dynamic routes. Browser smoke: `/` (HTTP 200, renders "PaperScout trends" header + "No papers in this run were marked RECOMMEND" — the Phase 2 dev DB has 30 collected papers but no ingested evaluations, so the empty-recommended fallback is exercised), `/library` (200), `/runs/<id>` + `?showAll=1` (200, paper links present), `/papers/<id>` (200, renders abstract + links section; score breakdown correctly absent because dev DB has no evaluations), `/papers/<bogus-uuid>` (dev returns 200 but body renders the not-found page; production build will return 404). Visual mobile/desktop overlap not verified beyond responsive class structure (`max-w-6xl`, `flex-wrap`, `grid lg:grid-cols-3`).
- **Plan housekeeping** — every Phase 4 README checklist item ticked. `<AppHeader />` global in layout was confirmed with the user during planning, as was the new `trends.ts` location.
- **Deferred (Phase 5 boundary)** — feedback star/comment controls; `/library?tags=...` route handler (tag chip links are emitted now so the contract is fixed); cross-run evaluation reuse via `evaluationsRepo.findReusable(...)` still pending UX direction; visual regression / Playwright coverage of the new pages.

Decision: **GO** to Phase 5 (feedback + filtering).

## Phase 3 — Skill output ingest + ranking closed

- Hardened `scripts/ingest.ts` as the only DB writer for skill outputs (`candidates.json` + `evaluations.json`). The script now: validates, dedups, persists, ranks densely, marks recommendations, and fails fast on contract violations.
- Extracted pure ranking helpers to `scripts/ingest/lib.ts`: `recomputeTotal`, `chooseRankingScore`, `rankPapers`. All three are zero-IO and unit-testable. Score selection is deterministic — `FULL_PDF SUCCESS` beats `ABSTRACT_SCREENING`; `FULL_PDF FAILED/UNAVAILABLE` falls back to `ABSTRACT_SCREENING` when present; otherwise the `FULL_PDF` Stage-1-preserved scores are used (the F5 path). Tie-breakers are documented and stable: score desc → recommendationDecision priority (`RECOMMEND > STORE_ONLY > LOW_QUALITY`) → `candidateOrder` → `paperId`.
- `is_recommended` semantics decided: `rank ≤ min(10, rankedCount) AND recommendationDecision === 'RECOMMEND'`. Resolves the README's internal conflict between "Mark exactly min(10, rankedCount) rows" and "recommendation count = 3 for the fixture set, matching the skill output decisions". The combined rule gives sample → 2 recommended (2 RECOMMEND + 1 STORE_ONLY), fixture → 3 recommended (F1, F3, F4); a real 30-paper run still produces up to 10 recommended rows when all top-10 are RECOMMEND. The README's "sample = 3 recommended" bullet was the stale one and is now updated transitively by the integration-test outcome.
- Bug fixes ported from `src/server/pipeline/persist.ts`: replaced `sourcesRepo.exists(paperId, source)` with `sourcesRepo.existsIdentity({...})` at both call sites (alternate-source identity is now keyed on `sourcePaperId`/`sourceUrl`, not just the source enum). Added a `seenPaperId` Set guard so two candidates that resolve via fuzzy match to the same Paper no longer violate `paper_run_results` uniqueness — the second is counted as `skipped` and the log line reports it.
- New fail-fast checks: duplicate evaluation rows for the same `(joinKey, evaluationStage)` and (after dedup resolution) for the same `(paperId, evaluationStage)`; every persisted paper must have at least one evaluation row, with a clear error including the offending joinKey. A future `--allow-partial` flag is mentioned in code comments but not implemented.
- Schema migration `20260511144744_add_recommendation_reason` adds nullable `paper_evaluations.recommendation_reason TEXT`. Decision recorded with user: prefer a real column over dropping the field or merging into `rankingExplanation`. `evaluationsRepo.upsert` widened to accept and write it; the ingest script passes `e.recommendationReason` through.
- Tests: `tests/unit/ingest/lib.test.ts` (14 tests) covers `recomputeTotal`, all 6 `chooseRankingScore` paths, every tie-breaker level, the F1>F3>F4>F2>F5 reproduction, and the recommendation cap. `tests/integration/ingest.test.ts` (6 tests, gated on `RUN_INTEGRATION=1` + `DATABASE_URL_TEST`) covers: the sample run end-to-end via `tsx scripts/ingest.ts` as a child process, idempotency + `--force`, the frozen Phase 2.5 reference run with full per-paper expectations + coarse flags, alternate-source `joinKey` routing through `additionalSources`, within-run fuzzy collision producing exactly one `paper_run_results` row, and fail-fast on missing evaluation. The integration test copies run dirs to `os.tmpdir()` so the reference dir stays read-only.
- Verification: `npm test` 107/107 green (was 91 + 14 new + 2 pre-existing prompt-eval counts shifted with manifest additions); `npm run test:integration` 7/7 green (1 prior collect→persist + 6 new ingest); `npm run lint` clean; `npx tsc --noEmit` clean; `npm run build` green.
- Plan housekeeping: ticked Phase 3 README boxes that landed (ingest semantics, evaluation persistence, regression fixtures, tests). Deleted `plan/phase-3-ranking/outdated-report.html` — it was a stale snapshot of the pre-rewrite Phase 3 README and no longer applies after the skills+ingest pivot.
- Deferred (out of Phase 3 scope per the README): cross-run evaluation reuse via `evaluationsRepo.findReusable(...)` (needs UX direction first); app-side code-link extraction from PDFs (skill output doesn't expose extracted links); `--allow-partial` ingest mode; local-PDF support in the `evaluate-papers` skill (Phase 2.5 follow-up); removing `.agents/skills/evaluate-papers/` duplicate.

Decision: **GO** to Phase 4 (recommendation UI).

## Phase 2.5 — Prompt harness closed

- Built an offline harness around the `evaluate-papers` Claude Code skill. No app-level Anthropic SDK, no DB, no UI — fixture builder + per-record schema check + soft-bound checker + regression-snapshot writer. Per the locked architecture decision, prompt execution stays in the skill; the harness validates output and freezes references.
- Authored 5 fixtures (`scripts/prompt-eval/fixtures/F{1..5}/{metadata,bounds}.json`) covering the quality spectrum: F1 SAM (2304.02643, strong novelty + rigor), F2 EfficientFormerV2 (2212.08059, incremental tweak), F3 ViT-22B (2302.05442, famous lab + thin novelty), F4 SeaThru-NeRF (2304.07743, unknown lab + strong method), F5 The Dawn of LMMs / GPT-4V (2309.17421, hype-driven + weak claims). Each metadata.json carries a `_fixture` provenance block (`metadataSourceUrl`, `authoredAt`, `frozen: true`) and a verbatim arXiv abstract — no paraphrases, no fabricated text.
- `scripts/prompt-eval/lib.ts` is the single source of truth: `BoundsSchema` (zod), `FixtureManifestSchema`, `loadFixtures`, `buildCandidateMap` (mirrors `scripts/ingest.ts:65-79` including additionalSources), `resolveJoinKey` / `recomputeTotal` / `applyBounds` / `checkRecordSchema` / `summarize`. All public functions take raw `unknown` so a single malformed record cannot blank the diagnostic report. Per-record validation uses `EvaluationSchema.safeParse` rather than the file-level array schema.
- BoundsSchema uses `z.partialRecord(BoundedScoreKey, NumericRange)` (zod v4) — `z.record` with an enum key requires *all* enum keys in v4 and would reject any partial bounds. Caught at first smoke run.
- `scripts/prompt-eval/build-fixture-run.ts` emits `candidates.json` + `fixtures-manifest.json` atomically into `scripts/prompt-eval/runs/<YYYY-MM-DD-HHMM>/`. Manifest is a first-class output (`fixtureId`, `primaryKey`, `allJoinKeys[]`, `metadataPath`, `boundsPath`) — the checker reads only the manifest, never re-derives from `fixtures/`.
- `scripts/prompt-eval/check-evaluations.ts` validates schema per-record, applies bounds, prints schema/scores/bounds tables, the actual ranking, and three coarse flags (`f1InTop2`, `f5InBottom2`, `f4NotLast`). Exits 0 iff schema-valid for all + ≥4/5 bounds + zero unmatched joinKeys.
- `scripts/prompt-eval/normalize-evaluations.ts` reorders keys deterministically, recomputes `scores.total` only in the normalized copy, and writes `reference/normalized/F<n>.json` per fixture for regression diffs.
- `package.json` gained `prompt:fixtures`, `prompt:check`, `prompt:normalize` (no env-file flag — no DB). `.gitignore` adds `/scripts/prompt-eval/runs/` and `/scripts/prompt-eval/fixtures/**/paper.pdf` (per the user's "no PDFs in git" choice; local-PDF support in the skill is deferred — see below).
- Tests: `tests/unit/prompt-eval/bounds.test.ts` (8 tests) covers each committed bounds.json, empty bounds, unknown top-level keys, unknown dimension keys, malformed `min`, NumericRange requirement, `in` arrays, and null inside `pdfAnalysisStatus.in`. `tests/unit/prompt-eval/check-evaluations.test.ts` (25 tests across 7 describe blocks) covers the full defensive surface — joinKey hits via additionalSources, garbage-input safety, total-mismatch on a raw object schema would have rejected, bounds-fail with reason, schema-error-path reporting, manifest-driven fixture lookup, coarse-flag computation. `npm test` 91/91 green (was 58 + 33 new). `npm run lint` and `npx tsc --noEmit` clean.
- `.agents/skills/evaluate-papers/SKILL.md` re-synced byte-for-byte with `.claude/skills/evaluate-papers/SKILL.md`. Pre-sync diff was only the PRD §13 median-affiliation rule (Phase 1 backlog item missing from `.agents/`). Decision on whether to retain `.agents/` at all is deferred to Phase 3 cleanup.
- Manual eval loop converged on the first iteration. `npm run prompt:fixtures` → run dir `scripts/prompt-eval/runs/2026-05-10-2142/`. `/evaluate-papers` invoked via the Skill tool against the explicit run dir. Stage 2 PDF downloads succeeded for F1–F4 (SAM 14.9 MB, EfficientFormerV2 0.87 MB, ViT-22B 5 MB, SeaThru-NeRF 9.3 MB); F5 (Dawn of LMMs) PDF is 45.6 MB → exceeds the 32 MB cap, correctly fell back to `pdfAnalysisStatus = "UNAVAILABLE"` with Stage-1 scoring preserved. All 4 SUCCESS PDFs read via Claude Code's native `Read` tool with `pages: 1-N`.
- Final scores: F1=86 RECOMMEND, F3=73 RECOMMEND, F4=71 RECOMMEND, F2=61 STORE_ONLY, F5=30 LOW_QUALITY. Ranking actual `F1 > F3 > F4 > F2 > F5` vs prior `F1 > F4 > F3 > F5 > F2` — F3/F4 swap is small and intuitive (ViT-22B's downstream breadth nudges it above SeaThru-NeRF), and F2 > F5 is correct because F2 has real benchmarks while F5 has none. All three coarse flags pass: F1 top-2 ✓, F5 bottom-2 ✓, F4 not-last ✓.
- Bounds: 5/5 pass (exceeded the ≥4/5 stop bar). F1's `pdfAnalysisStatus.in: ["SUCCESS","UNAVAILABLE"]` widening (Codex review item) wasn't load-bearing — Stage 2 succeeded — but the F5 bound proved its value when the 45.6 MB PDF was rejected.
- Reference outputs saved: `scripts/prompt-eval/reference/raw/2026-05-10-2142/{candidates,evaluations,fixtures-manifest}.json` plus `scripts/prompt-eval/reference/normalized/F{1..5}.json`. These are the regression anchor for Phase 3 prompt tuning.
- Codex review caught one blocker pre-implementation (local fixture PDFs would have been inert because the skill curls `pdfUrl` to `/tmp`) and several refinements (manifest as first-class output, raw-JSON diagnostic path, coarse ranking flags, fixture provenance). All applied. Local-PDF support in the skill is deferred — out of Phase 2.5 scope.
- Out of scope (deferred): teaching the skill to prefer a local run-dir PDF when present (would let F1/F4 re-tighten the `pdfAnalysisStatus` bound to `SUCCESS`-only); deletion of `.agents/skills/evaluate-papers/` if it stays canonical.

Decision: **GO** to Phase 3 (ranking pipeline).
