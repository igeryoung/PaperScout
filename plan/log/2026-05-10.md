# Log — 2026-05-10

## Phase 0 cleanup — build baseline separated from Docker

- Removed `ANTHROPIC_API_KEY` from Phase 0 app/env setup. V1 uses Claude Code skills + ingest, so the Next.js app does not need an Anthropic SDK key in `.env.local`.
- Reclassified Docker/Postgres checks as manual runtime DB verification. They are still needed before DB ingest/R4 closure, but they do not block `npm test`, `npm run lint`, or `npm run build`.
- Added unit coverage for readable `DATABASE_URL` validation errors.
- Updated Phase 0 README and STATE.md to reflect build-complete vs runtime-pending status.
- Verification: `npm test` 29/29, `npm run lint`, and `npm run build` passed. The first build attempt hit sandboxed Google Fonts network access; rerun with approved network access passed. No Docker commands were run for verification.

## Local Postgres port moved to 5435

- Updated Docker Compose to publish Postgres on host port `5435` instead of `5432`, avoiding conflicts with an existing local Postgres on the default port.
- Updated `.env.example` and local `.env.local` to use `postgresql://paperscout:paperscout@localhost:5435/paperscout?schema=public`.
- Docker daemon was unavailable from this shell, so a local non-Docker Postgres cluster was initialized under ignored path `.local/postgres-5435`.
- Started the local cluster on port `5435`, created role/database `paperscout`, and verified `psql postgresql://paperscout:paperscout@localhost:5435/paperscout -c 'SELECT current_database(), current_user, 1'` succeeds.
